source "https://rubygems.org"

ruby "3.2.3"

gem 'rake'
# Use sass directly instead of compass
gem 'sass', '~> 3.7.4'
gem 'sass-globbing'

# Add these gems for development
group :development do
  gem 'guard'
  gem 'guard-livereload'
  # Required for guard-livereload
  gem 'rack'
  gem 'em-websocket'
end
